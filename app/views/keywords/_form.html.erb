<%= form_for(@keyword, remote: true) do |f| %>
  <% if @keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
      <% @keyword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :definition %><br>
    <%= f.text_area :definition %>
  </div>
  <div class="field">
    <%= f.label :topic_id %>
    <%= f.collection_select(:topic_id, Topic.order(:subject), :id, :subject, {:prompt => 'Select'}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
