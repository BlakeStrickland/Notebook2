<h1>Dashboard</h1>

<!-- <div class="dash_notes"><%= render @notes %></div>
<div class="dash_keywords"><%= render @keywords %></div>
<div class="dash_questions"><%= render @questions %></div> -->

<%= link_to 'New Topic', new_topic_path %>

<%= @topics.each do |topic| %>
  <p> <%= link_to topic.subject, notes_path %> </p>
<% end %>
